const sum = (a, b) => a + b;
const subtracting = (a, b) => a - b;
const multiply = (a, b) => a * b;
const dividing = (a, b) => a / b;

module.exports = {
    sum,
    subtracting,
    multiply,
    dividing
}